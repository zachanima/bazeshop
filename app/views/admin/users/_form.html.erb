<%= form_for ['admin', @shop, @user] do |f| %>
  <fieldset>
    <ol>
      <li>
        <%= f.label :login %>
        <%= f.text_field :login %>
      </li>
      <li>
        <%= f.label :password %>
        <%= f.text_field :password, value: @user.plaintext_password %>
      </li>
      <li>
        <%= f.label :email %>
        <%= f.email_field :email %>
      </li>
    </ol>
  </fieldset>

  <fieldset>
    <%= f.submit %>
  </fieldset>
<% end %>
